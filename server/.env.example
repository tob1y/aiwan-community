# 本机 Docker Postgres 示例见项目根目录 docker-compose.yml
DATABASE_URL="postgresql://aiwan:aiwan_secret@127.0.0.1:5433/aiwan?schema=public"

# 生产务必改为随机长串
JWT_SECRET="请改成随机长字符串"

# 浏览器访问前端的地址（CORS）
FRONTEND_ORIGIN="http://localhost:5173"

PORT=8787
